replicaCount: 1
resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
ingress:
  hosts:
    - host: app.dev.example.com
      paths:
        - path: /
          pathType: Prefix
configMap:
  data:
    APP_ENV: development
