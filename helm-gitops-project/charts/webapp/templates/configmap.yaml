apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
data:
  APP_ENV: {{ .Values.environment | default "development" }}
  APP_PORT: "{{ .Values.port }}"
  APP_HOST: "{{ .Values.ingress.hostnames | first | default "localhost" }}"


